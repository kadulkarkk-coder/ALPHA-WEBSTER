# Webster Alpha environment configuration
# Keep the real key in a local .env file. Never commit secrets.

GEMINI_API_KEY=

# Optional: override the default Gemini model.
# WEBSTER_GEMINI_MODEL=gemini-2.5-flash
