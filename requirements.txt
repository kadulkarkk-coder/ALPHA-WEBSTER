# Webster Alpha
# Free/local voice stack. No ElevenLabs and no PyAudio.
# Ollama itself is installed separately as a local service.

# Ollama HTTP communication
requests>=2.32.0,<3

# Environment/configuration
python-dotenv>=1.0.1,<2

# Local microphone capture + VAD
sounddevice>=0.5.5,<1
numpy>=2.0,<3

# Local speech-to-text (Whisper)
faster-whisper>=1.2.1,<2

# Offline Windows/macOS/Linux text-to-speech
pyttsx3>=2.99,<3
