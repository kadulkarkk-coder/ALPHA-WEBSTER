# Webster Alpha voice/core dependencies
# Gemini is the primary AI provider; Ollama remains an optional local fallback.

google-genai>=1.0.0,<2
requests>=2.32.0,<3
python-dotenv>=1.0.1,<2

# Voice input: PyAudio is the preferred microphone transport.
# sounddevice remains available as a fallback backend.
PyAudio>=0.2.14,<0.3
sounddevice>=0.5.5,<1
numpy>=2.0,<3
faster-whisper>=1.2.1,<2
pyttsx3>=2.99,<3
